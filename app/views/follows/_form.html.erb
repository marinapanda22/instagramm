<%= simple_form_for(@follow) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :follower_id %>
    <%= f.input :following_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
